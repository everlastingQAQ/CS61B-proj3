# PROJECT STATUS

> 更新日期：2026-09-17  
> 本文记录当前工作区代码的实际状态，包含尚未提交的修改。

## 当前目标

游戏采用回合制：玩家每成功移动一格，物品交互和怪物回合才会结算。地图中有 5 种互不重复的遗物；拾取任意 3 个后应激活出口，玩家存活并到达出口后获胜。每个遗物都会激活一种固定怪物，第 4、5 个遗物还会根据遗物类型给予固定 Buff。

目前物品—怪物—Buff 主流程、多怪物行动、伤害、HUD 状态、情境提示和持久化已经接入。出口激活、胜负状态、结束画面和 Radar 的方向提示仍未实现。

## 工作区交接

- 当前基准提交为 `88c35e9 finish monster ai`。
- 本文记录的物品、多怪物、伤害、Buff、HUD 和测试改动都还在工作区中，尚未提交；接手时不要重置或覆盖现有修改。
- `PROJECT_STATUS.md`、测试目录、Buff/怪物辅助类、Noto Sans SC 字体及授权文本目前是未跟踪文件，其余相关源码是已修改文件。
- 新增字体资源为 `assets/font/NotoSansSC-VF.ttf`，授权文本为 `assets/font/OFL-NotoSansSC.txt`。
- 开始下一阶段前先运行 `git status --short`，确认这些修改仍然存在。

## 当前回合流程

一次有效玩家回合按以下顺序执行：

1. `Player` 尝试执行 W/A/S/D 移动；撞墙或其他无效输入不推进回合。
2. `Engine.resolvePlayerInteractions()` 拾取当前位置的遗物。
3. 拾取遗物后立即创建对应怪物；第 4、5 次拾取同时应用对应 Buff。
4. 若 Stun 仍有剩余回合，本回合所有怪物跳过行动；否则由 `MonsterController` 结算全部怪物。
5. 所有怪物基于回合开始时的同一位置快照计算移动意图。
6. `MonsterController` 统一解决占位和目标冲突，再执行合法移动。
7. 怪物尝试进入玩家格时保持原位，并把接触事件按稳定 ID 顺序交给 `Engine` 结算。
8. 一层 Shield 抵挡一次接触伤害；否则玩家失去 1 HP。HP 最低为 0。
9. `Engine` 更新玩家视野、已探索区域和 HUD 情境提示。

新生成的怪物会参加本次拾取所在的怪物回合；若本次获得 Stun，则所有怪物跳过该回合。

## 物品、怪物与 Buff

五种遗物在新游戏中各生成一个，映射固定如下：

| 遗物 | 字符 | 激活怪物 | 第 4、5 次拾取时获得的 Buff |
|---|---:|---|---|
| Crystal | `*` | Hunter | Vision：视野半径永久 +2 |
| Coin | `$` | Coward | Heal：恢复 1 HP，不超过最大 HP |
| Key | `k` | Guardian | Radar：记录为已激活，方向提示尚未渲染 |
| Orb | `o` | Ambusher | Shield：增加 1 次伤害抵挡 |
| Rune | `?` | Patroller | Stun：跳过 2 个怪物行动回合 |

Buff 是否发放只取决于本次拾取后的总数是否大于 3，因此前三个遗物只生成怪物，第 4、5 个遗物同时生成怪物并发放自身对应的 Buff。

怪物出生在可行走且未被玩家、其他怪物或剩余遗物占据的位置。优先选择与玩家曼哈顿距离至少 10 的位置；若地图上没有这样的候选位置，则退回任意合法位置。Guardian 的守护点设为触发它的 Key 原位置。

## 多怪物与 AI

`Engine` 使用 `List<Monster> monsters`；空列表表示尚未激活怪物。新游戏不再创建测试怪物，每次拾取才新增一只。每只怪物拥有稳定 ID，`nextMonsterId` 随游戏保存，加载后不会复用 ID。

| 怪物 | 当前行为 |
|---|---|
| Hunter | 锁定玩家位置 2 回合，并通过普通 BFS 前往锁定位置 |
| Ambusher | 预测玩家最近移动方向前方最多 3 格，将预测位置锁定 2 回合 |
| Guardian | 玩家到守护点的路径距离不大于 10 时追击；玩家离守护点或怪物离守护点的路径距离达到 20 时返回；追击目标锁定 2 回合 |
| Patroller | 计算当前连通区域内的两个远距离端点，并在两点间往返 |
| Coward | 与玩家路径距离不大于 4 时逃跑，不小于 7 时追击，5～6 格保持原状态；追击目标锁定 2 回合 |

所有追击状态由 `CommittedChase` 管理两回合目标锁定。`Pathfinder` 只考虑地形；怪物的临时占位和移动冲突由 `MonsterController` 处理。

当前冲突规则：

1. 两只怪物选择同一空格时，稳定 ID 较小者移动。
2. 回合开始时已被怪物占据的格子，本回合不能进入。
3. 怪物不能交换位置，也不能在同一格结束回合。
4. 怪物尝试进入玩家格时产生接触事件，自身保持原位。

这套规则会让狭窄走廊中的后方怪物多等待一回合。若试玩后希望队列移动更紧凑，可再允许怪物进入本回合已经腾出的旧位置。

## 渲染与 HUD

- 五种怪物使用独立字符和颜色：Hunter `H`、Ambusher `A`、Guardian `G`、Patroller `P`、Coward `C`。
- 物品和怪物只在当前可见范围内绘制；玩家始终绘制。
- HUD 左侧显示 `HP current / max`，中间显示当前生效的 Shield、Stun、Vision 和 Radar，右侧显示 `ITEMS collected / 3`；无 Buff 时显示 `STATUS NONE`。
- 收集数量超过 3 后，物品计数显示为红色。
- 一种怪物第一次进入视野时，左下角以无背景单行中文日志显示名称和行为说明 3 个有效回合；已见怪物类型随存档保存。
- 没有怪物首次提示时，玩家靠近遗物的曼哈顿距离不超过 3，左下角会显示该遗物对应的怪物和超额拾取 Buff；专有名词保留英文。
- 怪物首次提示优先于附近遗物提示；怪物提示结束后，只要玩家仍在遗物 3 格内，就会恢复遗物提示。
- 中文日志使用 Noto Sans SC 22px，运行时只生成当前文案需要的中文字形；顶部 HUD 继续使用 JetBrains Mono。
- 左下角日志没有面板背景，通过 2px 黑色阴影保持地图上的可读性。
- Radar 激活状态已经显示在 HUD 中，但出口尚未接入，因此还没有方向提示。

## 持久化

`GameSave` 当前保存：

- 世界、剩余物品和已探索区域；
- 玩家位置、最近一次成功移动方向、当前/最大 HP 和 Shield 次数；
- 全部怪物的 ID、位置、类型、状态、守护点、巡逻路线及锁定目标；
- 视野半径、随机数状态和 `nextMonsterId`；
- Stun 剩余回合数、Radar 激活状态和已经提示过的怪物类型。

空怪物列表可以正常保存和加载。对缺少新增字段的旧存档，怪物列表默认为空，玩家生命值默认为满血，Shield/Stun/Radar 默认为未激活，已见怪物类型默认为空，缺少视野半径时使用 7。项目仍没有存档格式版本号。

首次怪物提示的剩余显示回合数属于临时 UI 状态，不写入存档；存档只保存已经提示过的怪物类型。加载后，当前视野中尚未提示过的怪物会正常触发首次说明。

## 已知未完成项

1. 收集 3 个遗物后激活出口、到达出口获胜尚未实现。
2. 玩家 HP 归零后仍停留在 `PLAYING`，死亡状态和结束画面尚未实现。
3. 玩家可以主动走入怪物当前占据的格子；这一情况还没有即时伤害或阻挡规则。
4. Radar 只设置并保存 `radarActive`，尚未在 HUD 中显示出口方向。
5. Guardian 生成在远处、守护点却是已经拾取的 Key 位置，可能先长距离返回守护点；需要试玩确认体验。
6. Patroller 使用连通区域的最远可达 tile，而非房间或地图节点。
7. Pathfinder 的每次距离查询都会重新执行 BFS；怪物增多后的性能尚未测量。
8. 存档格式没有版本号或迁移层。

## 建议下一步

1. 实现出口定位、收集 3 件后的出口激活，以及到达出口后的胜利状态。
2. 增加死亡/胜利 `GameState` 和结束画面，并在 HP 归零后停止继续行动。
3. 明确并实现玩家主动走入怪物格的规则。
4. 把 Radar 的出口方向接入 HUD。
5. 试玩 Guardian 出生与守护点、走廊拥堵和五种 Buff 的实际体验。
6. 加入存档版本与必要的迁移或旧档清理策略。

## 当前验证状态

- 测试源码中共有 17 个 JUnit 测试。
- 覆盖物品固定映射与第 4/5 件 Buff 边界、玩家 HP/Heal/Shield、怪物出生规则、怪物外观、两回合目标锁定、移动冲突、接触事件、怪物状态存档，以及 Buff/已见怪物字段的 JSON 序列化。
- `./gradlew test --rerun-tasks` 已于 2026-09-17 通过：17 个测试，0 failure，0 error。
- `./gradlew lwjgl3:run` 已成功启动到游戏窗口，Noto Sans SC 能由 LibGDX 正常加载；验证后手动结束进程。
- 启动时出现 WSL/Mesa 的 EGL 驱动警告，但没有字体或游戏初始化异常。
- 左下角文案在实际移动、怪物进入视野和靠近遗物时的视觉效果，以及怪物拥堵和 Buff 体验，仍需要完整手动试玩。
